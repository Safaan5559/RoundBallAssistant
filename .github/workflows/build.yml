name: Build Round Ball Assistant

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Build mod
        run: ./gradlew build
      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: round-ball-assistant
          path: build/libs/*.jar
          if-no-files-found: error
